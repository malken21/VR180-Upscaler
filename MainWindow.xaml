<Window x:Class="VR180_Upscaler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VR180 Upscaler v1.1.0" Height="450" Width="800"
        Icon="Assets/icon.png"
        AllowDrop="True" Drop="Window_Drop">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Scale Factor:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="MultiplierComboBox" SelectedIndex="1" Width="80" Margin="0,0,20,0">
                <ComboBoxItem Content="1x"/>
                <ComboBoxItem Content="2x"/>
                <ComboBoxItem Content="4x"/>
            </ComboBox>

            <TextBlock Text="Execution Provider:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="ProviderComboBox" SelectedIndex="0" Width="250">
            </ComboBox>
        </StackPanel>
        
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="2" CornerRadius="5" Height="100" Margin="0,0,0,10" Background="#FFF0F0F0">
            <TextBlock Text="Drop VR180 Video File Here" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="16"/>
        </Border>
        
        <TextBox x:Name="LogTextBox" Grid.Row="2" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas"/>
    </Grid>
</Window>
